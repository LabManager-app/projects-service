apiVersion: v1
kind: Service
metadata:
  name: projects-service
spec:
  type: NodePort
  selector:
    app: projects-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 30003
